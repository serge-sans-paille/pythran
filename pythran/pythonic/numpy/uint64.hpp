#ifndef PYTHONIC_NUMPY_UINT64_HPP
#define PYTHONIC_NUMPY_UINT64_HPP

#include "pythonic/utils/proxy.hpp"

namespace pythonic {

    namespace numpy {

        uint64_t uint64() {return uint64_t();}
        PROXY(pythonic::numpy, uint64);

    }

}

#endif

